import { Color } from "ora";

export type StyleConfig = {
  errorColor?: string | Color;
  warrningColor?: string | Color;
  spawnColor?: string | Color;
  succeedColor?: string | Color;
  pausedcolor?: string | Color;
  messageColor?: string | Color;
  textColor?: string | Color;
}
